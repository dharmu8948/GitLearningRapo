#include<stdio.h>
int main()
{
	int n;
	scanf("%d",&n);
	int a[n];
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	for(i=0;i<n;i++)
	{
		a[0]=min;
		a[0]=max;
		for(j=i+1;j<n;j++)
		{
			if(min>a[j])
			{
				min=a[j];
			    if(a[j]==a[i+1])
			    count1++;
			}
			if()
		}
	}
}
